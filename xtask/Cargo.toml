[package]
name = "xtask"
version = "0.0.0"
edition = "2024"
publish = false
description = "Repo automation tasks (harness sync gate). Run via `cargo xtask <task>`."

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
serde = { workspace = true }
toml = "0.8"
