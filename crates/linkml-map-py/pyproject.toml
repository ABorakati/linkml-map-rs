[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "linkml-map-rs"
version = "0.1.0"
description = "Rust async/concurrent port of linkml-map"
requires-python = ">=3.8"
